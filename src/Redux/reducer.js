
const initialState = {
    bookmarks: [],
};

const app = (state = initialState, action) => {

    switch(action.type) {
        case "":
            return {};
        default:
            break;
    }
};